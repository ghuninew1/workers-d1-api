{
  "name": "@ghuninew1/workers-d1-api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "export NO_D1_WARNING=true && wrangler deploy --minify",
    "dev": "export NO_D1_WARNING=true && wrangler dev --port 3001",
    "devr": "export NO_D1_WARNING=true && wrangler dev --port 3000 -r ",
    "start-stackblitz": "WRANGLER_SEND_METRICS=false wrangler dev index.js --local"
  },
  "devDependencies": {
    "hono": "^3.5.6",
    "wrangler": "3.6.0"
  }
}
